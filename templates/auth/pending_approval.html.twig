{% extends 'base.html.twig' %}

{% block title %}{{ trans('auth.pending_approval_title') }} &mdash; {{ site_name }}{% endblock %}

{% block content %}
<h1>{{ trans('auth.pending_approval_title') }}</h1>

<p class="success">{{ trans('auth.pending_approval_body') }}</p>
{% endblock %}
